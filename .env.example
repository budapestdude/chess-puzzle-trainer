# Server Configuration
NODE_ENV=production
# Railway provides PORT automatically, don't set it manually
# PORT=3000

# Database Configuration
# For Railway: Use /app/data for persistent storage
DATABASE_PATH=/app/data/chess_puzzle_trainer.db

# CORS Configuration
# Comma-separated list of allowed origins for production
# For Railway: Update with your actual Railway domain
# Example: ALLOWED_ORIGINS=https://your-app.up.railway.app
# Leave empty to allow all origins (less secure)
# ALLOWED_ORIGINS=

# Security
# Add any API keys or secrets here (never commit actual values)
# JWT_SECRET=your-secret-key-here

# Logging
# LOG_LEVEL=info

# Railway-specific notes:
# - Railway automatically sets NODE_ENV=production
# - Railway provides PORT environment variable
# - Use Railway's persistent volume for database storage
# - Configure domain in Railway dashboard